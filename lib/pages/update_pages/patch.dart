import 'package:api_test/models/item.dart';
import 'package:flutter/material.dart';

class Patch extends StatelessWidget {
  final Item item;

  const Patch({Key key, this.item}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
