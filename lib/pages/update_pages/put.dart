import 'package:api_test/models/item.dart';
import 'package:flutter/material.dart';

class Put extends StatelessWidget {
  final Item item;

  const Put({Key key, this.item}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
